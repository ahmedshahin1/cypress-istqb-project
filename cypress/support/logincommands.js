Cypress.Commands.add("openLogin", () => { cy.get('[data-test="nav-sign-in"]').click(); cy.url().should('include', 'login'); }); Cypress.Commands.add("login", (email, password) => { cy.get('[data-test="email"]') .should('be.visible') .clear() .type(email, { delay: 50 }); cy.get('[data-test="password"]') .should('be.visible') .clear() .type(password, { delay: 50 }); cy.get('[data-test="login-submit"]').click(); })